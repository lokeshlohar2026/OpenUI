DATABASE SCHEMA & RELATIONSHIP MAP (Live PostgreSQL Engine):

TABLE RELATIONSHIPS & JOIN GRAPH (HOW TABLES CONNECT):
• mfi360_funds (Central Master Entity):
  - JOIN mfi360_amcs ON mfi360_funds.mf_id = mfi360_amcs.mf_id (Fund House / AMC)
  - JOIN mfi360_fund_plans ON mfi360_funds.fund_id = mfi360_fund_plans.fund_id (Direct/Regular Plans, Expense Ratios, Exit Loads)
  - JOIN mfi360_fund_portfolio_holdings ON mfi360_funds.fund_id = mfi360_fund_portfolio_holdings.fund_id (Monthly Stock & Debt Holdings)
  - JOIN mfi360_fund_aum_history ON mfi360_funds.fund_id = mfi360_fund_aum_history.fund_id (Historical AUM Growth Timeline)
  - JOIN mfi360_scheme_valuation_metrics ON mfi360_funds.fund_id = mfi360_scheme_valuation_metrics.fund_id (Portfolio P/E, P/B, Dividend Yield)
  - JOIN mfi360_fund_debt_metrics ON mfi360_funds.fund_id = mfi360_fund_debt_metrics.fund_id (Average Maturity, Duration, YTM)
  - JOIN mfi360_fund_manager_tenures ON mfi360_funds.fund_id = mfi360_fund_manager_tenures.fund_id (Scheme Management Tenures)
  - JOIN amfi_fund_benchmarks ON mfi360_funds.fund_id = amfi_fund_benchmarks.fund_id (Benchmark Mappings)

• mfi360_fund_plans (Scheme Plan Variants):
  - JOIN mfi360_scheme_risk_ratios ON mfi360_fund_plans.scheme_id = mfi360_scheme_risk_ratios.scheme_id (Risk Ratios: Sharpe, Beta, Standard Deviation)
  - JOIN amfi_isin_master ON mfi360_fund_plans.isin = amfi_isin_master.isin (AMFI Master Details)

• mfi360_fund_manager_tenures (Manager Tenures):
  - JOIN mfi360_fund_managers ON mfi360_fund_manager_tenures.fund_manager_id = mfi360_fund_managers.fund_manager_id (Manager Qualifications & Bio)
  - JOIN mfi360_amcs ON mfi360_fund_manager_tenures.amc_id = mfi360_amcs.mf_id (Manager AMC)

• Indices & Benchmark Trackers:
  - amfi_fund_benchmarks.index_id = amfi_portal_indices.index_id
  - amfi_portal_indices_history.index_id = amfi_portal_indices.index_id
  - mfi360_indices_history.index_id = mfi360_indices.index_id
  - mcx_icomdex_indices_history.index_id = mcx_icomdex_indices.index_id
  - mcx_spot_market_prices.commodity_id = mcx_spot_commodities.commodity_id

COMPLETE TABLE SPECIFICATIONS:

1. mfi360_fund_portfolio_holdings (Docker PostgreSQL table for mfi360_fund_portfolio_holdings | Live Rows: 9548521)
   • Columns:
     - fund_id [INTEGER, PK, FK -> mfi360_funds.fund_id]
     - scheme_id [INTEGER]
     - portfolio_date [DATE, PK]
     - company_name [VARCHAR(300), PK]
     - issuer_name [VARCHAR(300)]
     - company_isin [VARCHAR(20)]
     - sector_name [VARCHAR(200)]
     - instrument_name [VARCHAR(100), PK]
     - market_cap_caption [VARCHAR(50)]
     - quantity [NUMERIC(20, 4)]
     - market_value [NUMERIC(20, 4)]
     - percentage_in_net_asset [NUMERIC(8, 4)]
     - coupon_rate [NUMERIC(14, 4)]
     - maturity_date [DATE]
     - rating [VARCHAR(50)]
     - rating_agency [VARCHAR(100)]
     - is_new_in [BOOLEAN]
     - fetched_at [DATETIME]

2. mfi360_fund_manager_tenures (Docker PostgreSQL table for mfi360_fund_manager_tenures | Live Rows: 11139)
   • Columns:
     - fund_manager_id [INTEGER, PK, FK -> mfi360_fund_managers.fund_manager_id]
     - amc_id [INTEGER, FK -> mfi360_amcs.mf_id]
     - fund_id [INTEGER, PK]
     - fund_name [VARCHAR(300)]
     - from_date [DATE, PK]
     - to_date [DATE]
     - fetched_at [DATETIME]

3. mfi360_fund_plans (Docker PostgreSQL table for mfi360_fund_plans | Live Rows: 7764)
   • Columns:
     - scheme_id [INTEGER, PK]
     - fund_id [INTEGER, FK -> mfi360_funds.fund_id]
     - scheme_name [VARCHAR(300)]
     - plan [VARCHAR(10)]
     - option [VARCHAR(10)]
     - amfi_code [VARCHAR(20)]
     - amfi_amc_code [INTEGER]
     - isin [VARCHAR(20)]
     - launch_date [DATE]
     - expense_ratio [NUMERIC(6, 4)]
     - min_invest [NUMERIC(12, 2)]
     - exit_load [JSONB]
     - fetched_at [DATETIME]

4. mfi360_scheme_risk_ratios (Docker PostgreSQL table for mfi360_scheme_risk_ratios | Live Rows: 5690)
   • Columns:
     - scheme_id [INTEGER, PK, FK -> mfi360_fund_plans.scheme_id]
     - standard_deviation [NUMERIC(12, 4)]
     - beta [NUMERIC(12, 4)]
     - sharpe_ratio [NUMERIC(12, 4)]
     - fetched_at [DATETIME]

5. mfi360_funds (Docker PostgreSQL table for mfi360_funds | Live Rows: 2017)
   • Columns:
     - fund_id [INTEGER, PK]
     - mf_id [INTEGER, FK -> mfi360_amcs.mf_id]
     - fund_name [VARCHAR(300)]
     - nature [VARCHAR(50)]
     - sub_nature [VARCHAR(50)]
     - riskometer [VARCHAR(50)]
     - fund_manager [ARRAY]
     - aum_cr [NUMERIC(18, 4)]
     - aum_date [VARCHAR(20)]
     - portfolio_turnover_ratio [NUMERIC(10, 4)]
     - fetched_at [DATETIME]

6. mfi360_scheme_valuation_metrics (Docker PostgreSQL table for mfi360_scheme_valuation_metrics | Live Rows: 1977)
   • Columns:
     - fund_id [INTEGER, PK, FK -> mfi360_funds.fund_id]
     - portfolio_date [DATE]
     - price_to_earnings [NUMERIC(12, 4)]
     - price_to_book [NUMERIC(12, 4)]
     - dividend_yield [NUMERIC(12, 4)]
     - market_cap [NUMERIC(20, 4)]
     - portfolio_turnover_ratio [NUMERIC(12, 4)]
     - fetched_at [DATETIME]

7. mfi360_fund_debt_metrics (Docker PostgreSQL table for mfi360_fund_debt_metrics | Live Rows: 1977)
   • Columns:
     - fund_id [INTEGER, PK, FK -> mfi360_funds.fund_id]
     - portfolio_date [DATE]
     - average_maturity [NUMERIC(12, 4)]
     - average_maturity_unit [VARCHAR(20)]
     - yield_to_maturity [NUMERIC(12, 4)]
     - modified_duration [NUMERIC(12, 4)]
     - modified_duration_unit [VARCHAR(20)]
     - fetched_at [DATETIME]

8. mfi360_fund_managers (Docker PostgreSQL table for mfi360_fund_managers | Live Rows: 1206)
   • Columns:
     - fund_manager_id [INTEGER, PK]
     - name [VARCHAR(200)]
     - active [BOOLEAN]
     - educational_qualification [TEXT]
     - duplicate_of_fund_manager_id [INTEGER, FK -> mfi360_fund_managers.fund_manager_id]
     - fetched_at [DATETIME]

9. mfi360_fund_aum_history (Docker PostgreSQL table for mfi360_fund_aum_history | Live Rows: 0)
   • Columns:
     - fund_id [INTEGER, PK, FK -> mfi360_funds.fund_id]
     - aum_date [DATE, PK]
     - aum_cr [NUMERIC(18, 4)]
     - fetched_at [DATETIME]